runtime: java
env: flex
service: catalog

resources:
  cpu: 1
  memory_gb: 1

handlers:
- url: /.*
  script: this field is required, but ignored

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 1